# 🌐 Frontend环境变量配置模板
# 复制此文件为 .env.local 并填入你的实际配置值

# ===========================================
# 🔗 API配置 (必填)
# ===========================================

# 后端API基础URL
# 开发环境: http://localhost:8000
# 生产环境: https://your-api-domain.com
# Docker环境: http://backend:8000 (如果使用docker-compose网络)
VITE_API_BASE_URL=http://localhost:8000

# ===========================================
# 🔧 应用配置 (可选)
# ===========================================

# 应用标题
VITE_APP_TITLE=Job Agent Platform

# 应用描述
VITE_APP_DESCRIPTION=AI-powered job hunting assistant

# 应用版本
VITE_APP_VERSION=1.0.0

# ===========================================
# 🔐 认证配置 (可选)
# ===========================================

# JWT Token存储键名
VITE_TOKEN_STORAGE_KEY=access_token

# 用户信息存储键名
VITE_USER_STORAGE_KEY=user

# Token过期时间(小时)
VITE_TOKEN_EXPIRE_HOURS=24

# ===========================================
# 📊 功能开关 (可选)
# ===========================================

# 是否启用调试模式
VITE_DEBUG_MODE=false

# 是否显示开发工具
VITE_SHOW_DEV_TOOLS=false

# 是否启用分析功能
VITE_ENABLE_ANALYTICS=false

# ===========================================
# 🎨 UI配置 (可选)
# ===========================================

# 主题色
VITE_THEME_PRIMARY_COLOR=#3b82f6

# 是否启用暗黑模式
VITE_ENABLE_DARK_MODE=true

# 默认语言
VITE_DEFAULT_LANGUAGE=en

# ===========================================
# 📝 不同环境的配置示例
# ===========================================

# 本地开发环境 (.env.local):
# VITE_API_BASE_URL=http://localhost:8000
# VITE_DEBUG_MODE=true
# VITE_SHOW_DEV_TOOLS=true

# Docker开发环境 (.env.docker):
# VITE_API_BASE_URL=http://backend:8000
# VITE_DEBUG_MODE=false

# 生产环境 (.env.production):
# VITE_API_BASE_URL=https://api.yourdomain.com
# VITE_DEBUG_MODE=false
# VITE_ENABLE_ANALYTICS=true

# ===========================================
# 📋 配置说明
# ===========================================

# 1. Vite环境变量规则:
#    - 必须以 VITE_ 开头才能在客户端代码中访问
#    - 在代码中使用 import.meta.env.VITE_变量名 访问

# 2. 环境变量优先级:
#    .env.local > .env.production > .env > .env.example

# 3. 使用方法:
#    import.meta.env.VITE_API_BASE_URL

# 4. 安全注意事项:
#    - 不要在前端环境变量中存储敏感信息（如密钥、密码）
#    - 所有VITE_变量都会暴露给客户端
#    - 敏感配置应该放在后端

# ===========================================
# 🛠️ 快速配置指南
# ===========================================

# 1. 本地开发:
#    cp env.example .env.local
#    # 编辑 .env.local，设置 VITE_API_BASE_URL=http://localhost:8000

# 2. Docker部署:
#    # 在docker-compose.yml中直接设置环境变量
#    # 或创建 .env 文件供docker-compose使用

# 3. 验证配置:
#    npm run dev
#    # 在浏览器控制台检查: console.log(import.meta.env)
